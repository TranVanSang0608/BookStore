# Biến môi trường FE — Vite chỉ đưa các biến tiền tố VITE_ vào client bundle.
# LƯU Ý: giá trị ở đây là PUBLIC (nằm trong JS gửi tới browser) — không bao giờ đặt secret.
VITE_API_URL=http://localhost:3000/api
